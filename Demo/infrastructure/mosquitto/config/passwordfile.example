# This is only a placeholder.
# Generate the real `passwordfile` with:
#   powershell -ExecutionPolicy Bypass -File .\scripts\bootstrap_mqtt_broker.ps1 -BridgePassword '<strong-password>' -EdgePassword '<strong-password>'
